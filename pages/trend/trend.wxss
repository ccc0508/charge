/* pages/trend/trend.wxss */
page { background-color: #f7f8fc; }

.stats-header { display: flex; align-items: center; justify-content: space-between; padding: 20rpx 36rpx 12rpx; }
.stats-title { font-size: 40rpx; font-weight: 700; color: #1a1a2e; }
.month-selector { display: flex; align-items: center; gap: 16rpx; }
.nav-arrow { width: 52rpx; height: 52rpx; border-radius: 50%; background: #fff; display: flex; align-items: center; justify-content: center; font-size: 32rpx; color: #ff6b35; font-weight: 700; box-shadow: 0 2rpx 12rpx rgba(0,0,0,0.06); }
.nav-arrow:active { background: #fff5f0; }
.nav-title { font-size: 28rpx; font-weight: 600; color: #333; min-width: 100rpx; text-align: center; }

.section-card { margin: 20rpx 32rpx 0; background: #fff; border-radius: 24rpx; padding: 28rpx; box-shadow: 0 2rpx 16rpx rgba(0,0,0,0.04); }

.expense-color { color: #e74c3c; }
.income-color { color: #2ecc71; }

.bar-legend { display: flex; justify-content: center; gap: 40rpx; margin-bottom: 16rpx; }
.bar-legend-item { display: flex; align-items: center; gap: 8rpx; }
.bar-legend-dot { width: 20rpx; height: 20rpx; border-radius: 4rpx; }
.bar-legend-text { font-size: 24rpx; color: #666; }

.bar-scroll { width: 100%; white-space: nowrap; margin-bottom: 24rpx; }
.bar-canvas-inner { width: 900rpx; display: inline-block; }
.bar-canvas { width: 900rpx; height: 480rpx; }

.yearly-summary { display: flex; justify-content: space-around; padding: 24rpx 0 8rpx; border-top: 1rpx solid #f0f0f0; }
.yearly-summary-item { display: flex; flex-direction: column; align-items: center; gap: 8rpx; }
.yearly-summary-label { font-size: 22rpx; color: #999; }
.yearly-summary-value { font-size: 28rpx; font-weight: 700; }

.empty-state { display: flex; flex-direction: column; align-items: center; padding: 120rpx 0; }
.empty-icon { font-size: 80rpx; margin-bottom: 20rpx; }
.empty-text { font-size: 28rpx; color: #ccc; }
